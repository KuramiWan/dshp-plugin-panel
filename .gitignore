node_modules/
*.tsbuildinfo
.DS_Store
npm-debug.log*
test/.tmp/
# pnpm 本地 store（本仓库开发用 .tmp/xdg 作为 store-dir），不进版本库
.tmp/
# 本地临时文档（spec / handoff 等），不进版本库
tmp/
lib/
# 本地开发根（DSH_HOME 隔离；含 profile/会话/凭证引用，不进 git）
.dsh-dev/
