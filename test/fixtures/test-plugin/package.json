{
  "name": "dshp-test-plugin",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Test plugin fixture: hot-pluggable via cordis.patch.yml. Validates plugin tab's hot toggle / disable / restart flow. NOT published.",
  "main": "index.js",
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  },
  "cordis": {
    "name": "dshp-test-plugin"
  },
  "peerDependencies": {
    "@deepseek-ai/cordis": "^4.0.1"
  }
}
